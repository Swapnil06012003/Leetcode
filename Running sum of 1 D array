class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        vector<int> sum;
        int summ=0;
        for(int i=0;i<nums.size();i++){
            summ+=nums[i];
            sum.push_back(summ);
        }
        return sum;
    }
};
